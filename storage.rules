rules_version = '2';
service firebase.storage {
  match /b/ion-hapapa-3507.firebasestorage.app {
    match /posts/{userUid}/{allPaths=**} {
      allow write: if request.auth != null && request.auth.uid == userUid;
      allow read: if request.auth != null;
    }
  }
}